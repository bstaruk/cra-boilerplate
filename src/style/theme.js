import { device, size } from './devices';

const theme = {
  /* Colors */
  color: {
    black: '#000',
    white: '#fff',
  },

  /* Font Families */
  font: {
    sans: '"PT Sans", sans-serif',
    serif: '"PT Serif", serif',
  },

  /* Sizes & Devices */
  size,
  device,
};

export default theme;
